import { Button } from "@/components/ui/button";

export default function Home() {
  async function getPosts(id: string) {
    // e
  }
  return (
    <main>
      <Button>Hello</Button>
      <Button variant={"link"}>mode</Button>
    </main>
  );
}
